# REAL brand assets — robrecio.com

Converted from Rob's supplied REAL print files (EPS/PDF) on 2026-06-30.

## Staged here (ready, NOT yet placed on the site)
- `work-hard-be-kind-cobalt.{svg,webp}` — "Work Hard. Be Kind." motto, navy/chalk
- `work-hard-be-kind-coral.{svg,webp}` — same motto, coral/chalk

Per the go-live deck, the motto is a culture accent to use SPARINGLY (small
personal-ethos touch, never the hero). Color + placement are Rob's call, so
these are staged but not inserted into any page yet.

## MISSING from the supplied `brand/real-assets/` folder (needed to finish deck §6)
1. **Standalone `real` wordmark** — the folder only had the WORK HARD motto
   lockup (which embeds a tiny stylized `real`) + the North America map. There
   is no clean `real` wordmark to build the footer "Brokered by Real Broker,
   LLC" lockup. Footer broker attribution is currently compliant TEXT instead.
2. **REAL brand guide with exact hex** — deck says do NOT ship approximations,
   so navy/aqua/coral site accents were NOT added. Need official hex first.

Map asset (`map-103023-Gray.pdf`): intentionally skipped per deck (corporate/
recruiting asset, irrelevant to a hyperlocal Gulf Coast client).
